## Greeting A New World
## declare {a}, source of salutations
## declare {b}, bringer of the new line

Main {
  PrintHi {
    stage(a, b);

    a.set(72);
    a.print_char;

    a.set(a + 1);
    a.print_char;

    b.set(10);
    b.print_char;

    unstage_all;
  }
}
